## 修改
- 删除分析结果框中的“展开全部/收起”按钮与相关状态。
- 将结果框设置为固定高度并始终显示纵向滚动条：`max-h-[70vh] overflow-y-scroll`。
- 保留“复制”和“下载”按钮。

## 影响文件
- `components/CodeBlock.tsx`：移除状态与按钮；更新容器样式为固定高度 + 始终显示滚动条。

## 验证
- 在 Video Replication 中生成长结果时，右侧滚动条始终可见，内容可完整滚动查看，无需展开。